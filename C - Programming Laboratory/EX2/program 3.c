#include <stdio.h>

void main()
{
    float height,width;
    printf("Enter the height:");
    scanf("%f",&height);
    printf("Enter the width:");
    scanf("%f",&width);
    printf("The perimeter if the rectangle is: %f",(2.0*height)+(2.0*width));
}
